# @js-ak/zip-password-stream

```bash
npm install @js-ak/zip-password-stream
```

Requires Node.js 20.19+. Works with both ESM and CommonJS:

```ts
import { isTest } from "@js-ak/zip-password-stream"; // ESM
```

```js
const { isTest } = require("@js-ak/zip-password-stream"); // CJS
```
